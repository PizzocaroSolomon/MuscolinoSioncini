/*
vale la sintassi di c++

String genera_bottone(String id, int codice_colore, [int larghezza, int altezza = 40])
per indicare che un argomento e' opzionale si usano le quadre. 

quando si chiama la funzione, si possono nominare i parametri.
Ma bisogna dichiarare i parametri nelle graffe (come se fosse una mappa)
genera_bottone(codice_colore:65)

le funzioni sono degli oggetti e si possono essere slavate alle variabili e 
dati in input ad altre funzioni
*/
void main(List<String> args) {}
